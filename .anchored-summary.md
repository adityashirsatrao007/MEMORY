## Goal
Mirror every VSCode capability (MCP servers, GPU acceleration, API keys, extensions, workflows) into Antigravity IDE, inject all global API keys into all tools, set up LLM pipelines, and report which free provider keys the user still needs

## Constraints & Preferences
- All tools must use same MCP servers, API keys, and GPU acceleration
- Antigravity IDE is the primary AI IDE; VSCode is secondary with Cloud Data Agent Kit
- All API keys from `~/.config/global-apikeys/keys.env` must be wired into opencode, Continue, Antigravity IDE, and VSCode
- Free providers with missing keys must be reported so user can supply them
- User runs zsh, not bash
- DeepSeek key has "Insufficient Balance" — rely on Gemini/Groq/Cerebras/NVIDIA/FreeLLM instead

## Progress

### Done
- **Created Antigravity IDE `settings.json`** at `~/.antigravity-ide/User/settings.json` — mirrors VSCode's GPU-accelerated minimal UI config (no animations, no suggestions, no hover, no minimap, no toolbar decorations, `"workbench.activityBar.location": "hidden"`, `"window.commandCenter": false`, inline + tab completion disabled, git autofetch + material icon theme)
- **Updated Antigravity IDE MCP config** at `~/.config/antigravity-ide/mcp_config.json` — injected real GitHub token (`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`), Firecrawl key (`fc-4e929ff9b0bf482c987f64fd9705463c`) with retry settings, added Cloudflare server (token `cfut_5Zr1FlS8EXRTBWQxZNAWMDXevXGYX0UydhrwH5Zb49a5522b`, account `3f9022f8a051bbfacc8ce6da8c5f3289`), kept sequential-thinking, docs, filesystem servers
- **Updated opencode global config** — added 3 missing providers (openrouter, huggingface, cohere) with their real keys from `keys.env`
- **Fixed Continue `config.json`** — replaced 3 placeholder keys with real ones (DeepSeek, Z.AI, Mistral), added 6 new providers (Groq, Fireworks, NVIDIA, Cerebras, OpenRouter, HuggingFace) with concrete models, set tab autocomplete to Gemini 2.5 Flash, disabled anonymous telemetry
- **FreeLLM API proxy verified working** — PM2-managed on port 3001, all 12 API keys loaded in DB (all "healthy" status), 89 models available via OpenAI-compatible endpoint
- **Fixed PM2 vs systemd conflict** — removed redundant systemd service, now managed solely by PM2 with `pm2 save`
- **Discovered missing keys are only 2, not 5** — `PERPLEXITY_API_KEY` and `GLIF_API_TOKEN` are truly empty; `GITHUB_TOKEN`, `CLOUDFLARE_API_KEY`, `COHERE_API_KEY` are actually populated in the second section of `keys.env`

### In Progress
(none)

### Blocked
- `PERPLEXITY_API_KEY` and `GLIF_API_TOKEN` are empty — user must supply these if needed
- Google Cloud ADC auth still pending — user must run `gcloud auth application-default login` in browser
- PM2 startup on boot requires sudo (`pm2 startup systemd`)

## Key Decisions
- Use Continue as Copilot (Gemini Code Assist deprecated/unified into Antigravity)
- Antigravity IDE settings exactly mirror VSCode's performance/minimal UI config
- FreeLLM API proxy (`http://localhost:3001/v1`, key `freellmapi-c72bebe9578ae453d5d77b79af6e988e19405950c2087632`) works as unified endpoint with 89 models across 12 providers
- All Continue placeholder keys overwritten with real keys from `keys.env`; only Gemini uses AI Studio key (`AIzaSyD...`) which differs from `keys.env`'s Google Cloud key (`AQ.Ab8...`) — both valid for different endpoints
- MCP servers in Antigravity use hardcoded env vars (not `promptString` inputs like VSCode) since prompts cannot be answered in headless setup
- FreeLLM API managed by PM2 (not systemd) — the `pm2 startup` command needs sudo to enable boot auto-start

## Next Steps
1. Configure GitLens AI integration in Antigravity IDE
2. Walk through Google Cloud Data Agent Kit setup
3. Ask user to supply `PERPLEXITY_API_KEY` and `GLIF_API_TOKEN` (the only 2 truly missing free provider keys)
4. Run `sudo pm2 startup systemd -u aditya --hp /home/aditya` for PM2 boot auto-start

## Critical Context
- **GPU**: NVIDIA GeForce GTX 1650 Ti (4GB), driver 595.71.05, CUDA 13.2 — persistence mode enabled
- **keys.env location**: `~/.config/global-apikeys/keys.env` — auto-sourced by `.zshrc`
- **Truly missing keys**: `PERPLEXITY_API_KEY` and `GLIF_API_TOKEN` only. All other keys (GITHUB_TOKEN, CLOUDFLARE_API_KEY, COHERE_API_KEY, FIRECRAWL_API_KEY, MISTRAL_API_KEY) are populated in the second section of keys.env despite being commented out in the first section
- **DeepSeek**: key has "Insufficient Balance" — avoid using; fall back to Gemini, Groq, Cerebras, NVIDIA, FreeLLM
- **FreeLLM API proxy**: PM2-managed, listens on `http://localhost:3001`, unified key `freellmapi-c72bebe9578ae453d5d77b79af6e988e19405950c2087632`, 89 models across 12 providers. Keys already loaded in SQLite DB, all healthy. Known behavior: binds to IPv6 `[::]:3001` (no EADDRINUSE despite earlier suspicion — the restarts were from manual `kill -9` interfering with PM2 management)
- **Antigravity IDE**: Electron-based VSCode fork v1.107.0 (app) / 2.0.4 (ideVersion), installed at `/opt/antigravity-ide/Antigravity-IDE/`, data folder `~/.antigravity-ide/`, already has 15 extension directories
- **opencode**: v1.17.8, global config at `~/.config/opencode/opencode.json` (11 providers + 2 MCP), project config at `MEMORY/opencode.json` (shadcn + firecrawl MCP)
- **Continue**: config at `~/.continue/config.json` — now has 12 models across 11 providers, tab autocomplete on Gemini 2.5 Flash, telemetry off
- **PM2 processes**: freellmapi (id 0), fcc-server (id 2), memory-dashboard (id 1) — all managed by PM2 God Daemon (PID 3422)

## Relevant Files
- `~/.config/global-apikeys/keys.env` — all API keys (17+ present, 2 missing)
- `~/.antigravity-ide/User/settings.json` — Antigravity IDE settings (just created, VSCode-mirrored)
- `~/.config/antigravity-ide/mcp_config.json` — Antigravity IDE MCP config (5 servers with real keys)
- `~/.config/Code/User/mcp.json` — VSCode MCP config (7 servers with promptString inputs)
- `~/.config/Code/User/settings.json` — VSCode settings (GPU-accelerated minimal, reference for Antigravity mirror)
- `~/.continue/config.json` — Continue config (12 models, all real keys, tab autocomplete set)
- `~/.config/opencode/opencode.json` — opencode global config (11 providers + 2 MCP servers)
- `~/.gemini/antigravity/scratch/freellmapi/` — FreeLLM API source directory
- `~/.pm2/` — PM2 process list and logs
- `/home/aditya/Desktop/Projects/MEMORY/opencode.json` — project-level opencode config (shadcn + firecrawl)
- `/home/aditya/Desktop/Projects/MEMORY/Makefile` — CI pipeline definition (lint, typecheck, test, validate, seed)
- `/opt/antigravity-ide/Antigravity-IDE/resources/app/product.json` — app metadata (`dataFolderName: ".antigravity-ide"`)
