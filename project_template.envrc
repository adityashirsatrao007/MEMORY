# Drop this .envrc in any project root and run `direnv allow`
# It will auto-load all global API keys + any project-specific overrides
source_env "$HOME/.config/global-apikeys/load_keys.sh"

# Project-specific overrides below (optional):
# DATABASE_URL="..."
# APP_SECRET="..."
